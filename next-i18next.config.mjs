export const i18n = {
  locales: ["en", "ar"], // Supported languages
  defaultLocale: "ar", // Default language
  localeDetection: false, // Disable automatic locale detection
  ns: ["translation"], // Ensure this matches the JSON filename
  defaultNS: "translation",
};
